# uv project configuration — Python projects only.
# Delete this file if the project is not a Python project.
# https://docs.astral.sh/uv/reference/settings/

# Supply chain soak window: exclude packages published within the last 7 days.
# Tier 1 override (critical security patch):
#   uv add package==version --exclude-newer "1 hour"
exclude-newer = "7 days"
